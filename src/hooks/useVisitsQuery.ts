import { useQuery } from '@tanstack/react-query';

// export default function useVisitsQuery() {
//   return useQuery({
//     queryKey: ['visits'],
//     queryFn: fetsVisits,
//     retry: 1,
//   });
// }
